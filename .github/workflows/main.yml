name: Build Android APK (Sukses)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v4

    - name: ☕ Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: 🐘 Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: 🛠️ Perbaiki Gradle Wrapper Otomatis
      run: gradle wrapper

    - name: 🔑 Berikan Izin Eksekusi
      run: chmod +x gradlew

    - name: 🔐 Generate Keystore Sementara
      run: keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "C=US, O=Android, CN=Android Debug"

    - name: 🚀 Proses Kompilasi (Build APK)
      run: ./gradlew assembleDebug

    - name: 📦 Upload File APK
      uses: actions/upload-artifact@v4
      with:
        name: Aplikasi-Idadiyah-Terbaru
        path: app/build/outputs/apk/debug/*.apk
